[Unit]
Description=Synchronize Mailcow TLS certificate from Traefik
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
User=root
ExecStart=/root/roomreserve/deploy/sync_mailcow_tls_from_traefik.sh
